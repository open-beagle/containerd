name: Build Release v2.0 Images

on:
  push:
    branches:
      - release-v2.0
  workflow_dispatch: # 允许手动触发

env:
  REGISTRY_ALIYUN: registry.cn-qingdao.aliyuncs.com
  BUILD_VERSION: v2.0.5
  NERDCTL_VERSION: v2.0.3

jobs:
  build:
    name: Build Images
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY_ALIYUN }}
          username: ${{ secrets.REGISTRY_USER_ALIYUN }}
          password: ${{ secrets.REGISTRY_PASSWORD_ALIYUN }}

      - name: Cross Build
        env:
          BUILD_VERSION: ${{ env.BUILD_VERSION }}
        run: |
          docker run --rm -v ${{ github.workspace }}:/go/src/github.com/containerd/containerd/v2 \
            -w /go/src/github.com/containerd/containerd/v2 \
            -e BUILD_VERSION=${{ env.BUILD_VERSION }} \
            ${{ env.REGISTRY_ALIYUN }}/wod/golang:1.23-alpine \
            bash -c "bash ./.beagle/build-cross.sh"

      - name: Loong64 Build
        env:
          BUILD_VERSION: ${{ env.BUILD_VERSION }}
        run: |
          docker run --rm -v ${{ github.workspace }}:/go/src/github.com/containerd/containerd/v2 \
            -w /go/src/github.com/containerd/containerd/v2 \
            -e BUILD_VERSION=${{ env.BUILD_VERSION }} \
            ${{ env.REGISTRY_ALIYUN }}/wod/golang:1.23-loongnix \
            bash .beagle/build-loong64.sh

      - name: nerdctl-amd64
        env:
          TARGET_ARCH: amd64
        run: |
          docker run --rm -v ${{ github.workspace }}:/go/src/github.com/containerd/containerd/v2 \
            -w /go/src/github.com/containerd/containerd/v2 \
            ${{ env.REGISTRY_ALIYUN }}/wod/nerdctl:v2.0.3-${{ env.TARGET_ARCH }} \
            bash -c "mkdir -p _output/linux/${{ env.TARGET_ARCH }} && cp /usr/local/bin/nerdctl _output/linux/${{ env.TARGET_ARCH }}/nerdctl"

      - name: nerdctl-arm64
        env:
          TARGET_ARCH: arm64
        run: |
          docker run --rm -v ${{ github.workspace }}:/go/src/github.com/containerd/containerd/v2 \
            -w /go/src/github.com/containerd/containerd/v2 \
            ${{ env.REGISTRY_ALIYUN }}/wod/nerdctl:v2.0.3-${{ env.TARGET_ARCH }} \
            bash -c "mkdir -p _output/linux/${{ env.TARGET_ARCH }} && cp /usr/local/bin/nerdctl _output/linux/${{ env.TARGET_ARCH }}/nerdctl"

      - name: nerdctl-loong64
        env:
          TARGET_ARCH: loong64
        run: |
          docker run --rm -v ${{ github.workspace }}:/go/src/github.com/containerd/containerd/v2 \
            -w /go/src/github.com/containerd/containerd/v2 \
            ${{ env.REGISTRY_ALIYUN }}/wod/nerdctl:v2.0.3-${{ env.TARGET_ARCH }} \
            bash -c "mkdir -p _output/linux/${{ env.TARGET_ARCH }} && cp /usr/local/bin/nerdctl _output/linux/${{ env.TARGET_ARCH }}/nerdctl"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push images for amd64
        uses: docker/build-push-action@v5
        with:
          context: .
          file: .beagle/dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-amd64
          build-args: |
            BASE=alpine:3
            VERSION=${{ env.BUILD_VERSION }}
            TARGETOS=linux
            TARGETARCH=amd64
          provenance: false
          sbom: false

      - name: Build and push images for arm64
        uses: docker/build-push-action@v5
        with:
          context: .
          file: .beagle/dockerfile
          push: true
          platforms: linux/arm64
          tags: |
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-arm64
          build-args: |
            BASE=alpine:3
            VERSION=${{ env.BUILD_VERSION }}
            TARGETOS=linux
            TARGETARCH=arm64
          provenance: false
          sbom: false

      - name: Build and push images for loong64
        uses: docker/build-push-action@v5
        with:
          context: .
          file: .beagle/dockerfile
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-loong64
          build-args: |
            BASE=alpine:3
            VERSION=${{ env.BUILD_VERSION }}
            TARGETOS=linux
            TARGETARCH=loong64
          provenance: false
          sbom: false

      - name: Create and push multi-arch manifest
        run: |
          docker manifest create ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }} \
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-amd64 \
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-arm64 \
            ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}-loong64
          docker manifest push ${{ env.REGISTRY_ALIYUN }}/wod/containerd:${{ env.BUILD_VERSION }}
